<nav class="navbar navbar-expand-lg navbar-dark bg-dark m-0 sticky-top">
    <a class="navbar-brand" href="#">ActuSéries</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="mx-auto">

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="{{path('homepage')}}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Actualités</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="#">Films</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="#">Séries</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="#">Animés</a>
                </li>

                {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link" href="/admin">Administration</a>
                </li>
                {% endif %}


            </ul>

            <form class="form-inline my-2 my-lg-0 mx-5" action="{{path('search')}}" method="POST">
                <input class="form-control mr-sm-2" name="_contenu" type="search" placeholder="Search"
                    aria-label="Search" />
                <button class="btn btn-outline-success my-2 my-sm-0" type="submit">
                    Rechercher
                </button>
            </form>
        </div>
    </div>
    <div class="float-right mx-auto">
        {% if is_granted('ROLE_USER') %}


        {% if app.user.avatar is not null %}

        {% if app.user.googleID is not null %}
        <img src="{{app.user.avatar}}" style="width:3em" class=" rounded-circle">

        {% else %}
        <img src="{{asset('images/users/' ~ app.user.avatar)}}" style="width:3em" class="  rounded-circle">

        {% endif %}


        {% endif %}
        <div class="btn-group dropdown">

            <button type="button" class="text-warning dropdown-toggle" data-toggle="dropdown" aria-haspopup="true"
                aria-expanded="false">
                {{app.user.pseudo}}
            </button>
            <div class="dropdown-menu">

                <a class="dropdown-item" href="{{path('profile_edit', {'id' : app.user.id})}}">Editer son profil</a>
                <a class="dropdown-item" href="#">Another action</a>

                <div class="dropdown-divider"></div>
                <a class="dropdown-item" href="{{path('app_logout')}}">Deconnexion</a>



            </div>
        </div>
        {% else %}

        <a class="btn btn-outline-danger" href="{{path('app_login')}}">Connexion</a>

        <a class="btn btn-outline-warning" href="{{path('register')}}">Inscription</a>

        {% endif %}


    </div>


</nav>
{% for label,messages in app.flashes %}
{% for message in messages %}
<div class="col-4 text-center float-right">
    <div class="alert alert-dismissible alert-{{label}}">
        <button type="button" class="close" data-dismiss="alert">&times;</button>
        {{message}}
    </div>
</div>
{% endfor %}
{% endfor %}