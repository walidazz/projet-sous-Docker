<nav class="navbar navbar-expand-lg navbar-dark bg-primary sticky-top">
    <a class="navbar-brand" href="{{path('homepage')}}">ActuSéries</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>


    <div class="collapse navbar-collapse" id="navbarSupportedContent">

        <div class="mx-auto">
            <ul class="navbar-nav ">
                <li class="nav-item">
                    <a class="nav-link"
                        href="{{path('article_category_list', {'libelle' : 'Actualités' })}}">Actualités</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{path('article_category_list', {'libelle' : 'Films' })}}">Films</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{path('article_category_list', {'libelle' : 'Séries' })}}">Séries</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{path('article_category_list', {'libelle' : 'Animés' })}}">Animés</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="">Contact</a>
                </li>
                {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link" href="/admin">Administration</a>
                </li>
                {% endif %}


            </ul>


        </div>


        <ul class="navbar-nav mr-5">
            <form class="form-inline my-1 my-lg-0 mx-4" action="{{path('search')}}">
                <input class="form-control mr-sm-2" name="search" placeholder="Search" aria-label="Search" />
                <button class="btn btn-outline-success  my-sm-0" type="submit">
                    Rechercher
                </button>
            </form>
            {% if is_granted('ROLE_USER') %}
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button"
                    data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <img src="{{asset('images/users/' ~ app.user.avatar) | imagine_filter('avatar') }}" width="40"
                        height="40" class="rounded-circle">
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                    <a class="dropdown-item" href="{{path('user_profile', {'id' : app.user.id})}}">Voir profil</a>
                    <a class="dropdown-item" href="{{path('profile_edit', {'id' : app.user.id})}}">Editer son
                        profil</a>
                    <a class="dropdown-item" href="{{path('app_logout')}}">Deconnexion</a>
                </div>
            </li>
            {% else %}
            <a class="btn btn-outline-danger mx-4" href="{{path('app_login')}}">Connexion</a>


            {% endif %}
        </ul>



    </div>





</nav>

<div class="container mx-auto">
    {% for label,messages in app.flashes %}
    {% for message in messages %}
    <div class="col-4 text-center float-right">
        <div class="alert alert-dismissible alert-{{label}}">
            <button type="button" class="close" data-dismiss="alert">&times;</button>
            {{message}}
        </div>
    </div>
    {% endfor %}
    {% endfor %}
</div>