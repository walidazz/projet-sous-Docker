<nav class="navbar navbar-expand-lg navbar-dark bg-primary sticky-top">
    <a class="navbar-brand" href="#">ActuSéries</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>


    <div class="collapse navbar-collapse" id="navbarSupportedContent">

        <div class="mx-auto">
            <ul class="navbar-nav ">
                <li class="nav-item">
                    <a class="nav-link" href="{{path('article_list', {'libelle' : 'Actualités' })}}">Actualités</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{path('article_list', {'libelle' : 'Films' })}}">Films</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{path('article_list', {'libelle' : 'Séries' })}}">Séries</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{path('article_list', {'libelle' : 'Animés' })}}">Animés</a>
                </li>

                {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link" href="/admin">Administration</a>
                </li>
                {% endif %}


            </ul>


        </div>
        {% if is_granted('ROLE_USER') %}

        <ul class="navbar-nav mx-auto">
            <form class="form-inline my-1 my-lg-0 mx-3" action="{{path('search')}}" method="POST">
                <input class="form-control mr-sm-2" name="_contenu" type="search" placeholder="Search"
                    aria-label="Search" />
                <button class="btn btn-outline-success  my-sm-0" type="submit">
                    Rechercher
                </button>
            </form>
            <li class="nav-item dropdown ">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button"
                    data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <img src="{{asset('images/users/' ~ app.user.avatar) | imagine_filter('avatar') }}" width="40"
                        height="40" class="rounded-circle">
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                    <a class="dropdown-item" href="{{path('user_profile', {'id' : app.user.id})}}">Voir profil</a>
                    <a class="dropdown-item" href="{{path('profile_edit', {'id' : app.user.id})}}">Editer son
                        profil</a>
                    <a class="dropdown-item" href="{{path('app_logout')}}">Deconnexion</a>
                </div>
            </li>
        </ul>

        {% else %}
        <a class="btn btn-outline-danger mr-2" href="{{path('app_login')}}">Connexion</a>

        <a class="btn btn-outline-warning mr-2" href="{{path('register')}}">Inscription</a>
        {% endif %}

    </div>





</nav>
{% for label,messages in app.flashes %}
{% for message in messages %}
<div class="col-4 text-center float-right">
    <div class="alert alert-dismissible alert-{{label}}">
        <button type="button" class="close" data-dismiss="alert">&times;</button>
        {{message}}
    </div>
</div>
{% endfor %}
{% endfor %}